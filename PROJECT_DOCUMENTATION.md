# MSPM0G5307 FreeRTOS 项目文档

## 代码助手工作规范 (System Prompt)

### 📋 文档记录要求
作为代码助手，在此项目中必须严格遵守以下工作规范，并随时更新：

#### 1. 硬件确认原则 ⚠️
- **舵机控制**: 编写舵机控制代码前，务必询问使用的是180°还是270°舵机
- **串口通信**: 注意UART1(115200bps)和UART2(9600bps)的波特率差异
- **线序核对**: 不同串口线序可能不同，通信异常时需重新核对原理图
- **引脚映射**: 代码中uart2实际对应硬件uart3，其他外设也需要核对映射关系

#### 2. 代码开发规范
- **任务集成**: 所有新功能必须在StartupTask.c中集成为FreeRTOS任务
- **代码风格**: 遵循现有代码风格和结构
- **调试信息**: 串口调试信息需要尽量简洁
- **测试验证**: 新功能开发完成后必须进行测试并记录结果

#### 3. 文档维护要求
- **实时更新**: 每次代码修改后立即更新相关文档
- **测试记录**: 详细记录测试过程、参数和结果
- **Bug追踪**: 记录遇到的问题、解决方案和经验教训
- **配置信息**: 完整记录硬件配置、引脚定义、参数设置

#### 4. 开发流程
1. **确认需求**: 明确硬件类型和功能要求
2. **代码实现**: 按照现有架构进行开发
3. **功能测试**: 验证功能正确性
4. **文档更新**: 记录实现方案和测试结果
5. **问题记录**: 更新Bug记录和解决方案

#### 5. 安全原则
- **备份验证**: 修改现有代码前先确认备份
- **增量开发**: 优先注释旧功能，验证新功能后再删除
- **错误处理**: 添加必要的错误检查和异常处理

---

## 项目结构说明

### 核心文件
- **Task/StartupTask.c**: FreeRTOS所有task集成的地方，基本逻辑都写在这里
- **Hardware/**: 所有硬件控制函数封装
- **ti_msp_dl_config.h**: 定义引脚功能配置

### 重要约定
- **zdt**: 张大头步进电机的缩写
- **UART映射**: 代码中的uart2实际对应硬件中的uart3，其他uart也需要对应
- **调试串口**: uart2专门用于和上位机通信，所有调试输出都从此串口输出

## 硬件接线说明
*待补充具体接线信息*

## ZDT步进电机控制思路

### 控制流程
ZDT步进电机通过Emm_V5协议进行控制，控制流程如下：

1. **使能电机**: `Emm_V5_En_Control(1, true, false)` - 使能步进电机
2. **速度控制**: `ZDT_SetSpeed(ZDT1, speed, acc)` - 设置速度和加速度
3. **方向控制**: 正数为正转，负数为反转
4. **立即停止**: `Emm_V5_Stop_Now(1, false)` - 紧急停止
5. **禁用电机**: `Emm_V5_En_Control(1, false, false)` - 关闭电机使能

### 控制参数
- **地址**: 默认使用地址1
- **速度范围**: 0-5000 RPM
- **加速度**: 0-255 (0表示直接到达目标速度)
- **方向**: 0=CW顺时针, 1=CCW逆时针

### 任务实现 (StartupTask.c:190-233)
```c
void ZDT_task(void *pvParameters)
{
    // 控制变量
    int16_t speed = 500;        // 目标速度
    uint8_t acc = 20;          // 加速度
    uint32_t counter = 0;      // 计数器
    
    // 控制流程
    // counter=100: 使能电机
    // counter=200: 正转500RPM
    // counter=400: 反转500RPM  
    // counter=600: 停止
    // counter=700: 禁用并重置循环
}
```

## 串口通信配置

### 重要波特率差异 ⚠️
- **UART1**: 115200 bps (高速调试)
- **UART2**: 9600 bps (标准调试)
- **注意**: 调试不同串口时务必更换对应波特率设置

### 线序问题 ⚠️
不同串口的线序可能不同（即使都是串口），如果通信效果不符合预期，务必重新核对原理图确认TX/RX接线。

### printf重定向配置
- **实现位置**: Hardware/UART/UART.c中的fputc函数
- **使用串口**: UART_2_INST (实际对应硬件UART3)
- **重定向代码**:
```c
int fputc(int ch, FILE *stream)
{
    while( DL_UART_isBusy(UART_2_INST) == true );
    DL_UART_Main_transmitData(UART_2_INST, ch);
    return ch;
}
```

### UART2硬件配置
- **硬件外设**: UART3 (代码中定义为UART_2_INST)
- **TX引脚**: GPIOB PIN_2 (IOMUX_PINCM15)
- **RX引脚**: GPIOB PIN_3 (IOMUX_PINCM16)
- **波特率**: 9600 bps
- **数据格式**: 8N1 (8数据位，无奇偶校验，1停止位)

### 串口使用约定
- **uart2 (UART3硬件)**: 上位机调试通信，所有printf输出
- **其他串口**: 电机控制等硬件通信 (如ZDT控制)

## 舵机控制系统

### 舵机型号与规格
项目支持两种S20F舵机，**使用前务必确认舵机类型**：

#### S20F 180°舵机
- **操作角度**: 180°
- **控制精度**: ±0.36°
- **接线**:
  - 橙线: 信号线
  - 红线: 正极线 (5~6.5V)
  - 棕线: 负极线

#### S20F 270°舵机  
- **操作角度**: 270°
- **控制精度**: ±0.54°
- **接线**:
  - 白线: 信号线
  - 红线: 正极线 (5~6.5V)
  - 黑线: 负极线

### 共同技术参数
- **工作电压**: 5~6.5V
- **减速比**: 268:1
- **外观尺寸**: 40×20×40mm
- **PWM信号电平**: 3.3V/5V兼容
- **脉冲宽度范围**: 500~2500μs
- **旋转方向**: 逆时针 (1500→2500μs)

### PWM控制原理
```
脉冲宽度对应角度：
- 500μs  → 起始位置
- 1500μs → 中心位置 (为什么从1500开始？这是舵机的标准中位)
- 2500μs → 最大角度位置

180°舵机: 500μs=0°, 1500μs=90°, 2500μs=180°
270°舵机: 500μs=0°, 1500μs=135°, 2500μs=270°
```

### 控制接口
```c
void Set_Servo(SERVOx Servo, float angle);  // 设置舵机角度
```

**参数说明**:
- `Servo`: SERVO1 或 SERVO2
- `angle`: 目标角度
  - 180°舵机: 0~180°
  - 270°舵机: 0~270°

### 使用示例
```c
// ⚠️ 使用前请确认舵机类型！

// 180°舵机控制
Set_Servo(SERVO1, 0);    // 0度位置
Set_Servo(SERVO1, 90);   // 中间位置  
Set_Servo(SERVO1, 180);  // 最大角度

// 270°舵机控制  
Set_Servo(SERVO2, 0);    // 0度位置
Set_Servo(SERVO2, 135);  // 中间位置
Set_Servo(SERVO2, 270);  // 最大角度
```

### 测试任务实现 (StartupTask.c:202-249)
270°舵机测试任务已成功验证，测试序列：
```c
void Servo_task(void *pvParameters)
{
    // 测试序列
    // counter=100: 0° (起始位置)
    // counter=200: 135° (中心位置)  
    // counter=300: 270° (最大角度)
    // counter=400: 90° (1/3位置)
    // counter=500: 180° (2/3位置)
    // counter=600: 循环重置
}
```

### 测试结果 ✅
- **舵机型号**: S20F 270°舵机
- **控制引脚**: SERVO3 (GPIO_Servo_C2 - GPIOA PIN_15)
- **角度范围**: 0-270°正常工作
- **控制精度**: 符合±0.54°规格
- **PWM计算**: `Angle/270*2000+500` 公式正确
- **串口输出**: 调试信息正常显示

## 小车电机控制系统

### 硬件架构
项目使用带霍尔编码器的直流电机驱动小车轮子，共2个电机：

#### 电机1 (MOTOR1) - 左轮
- **电源**: 正负极电源供电
- **编码器接口**: 
  - A相: PA25 (ENCODER_PIN_A1)
  - B相: PA14 (ENCODER_PIN_B1)
- **方向控制**:
  - M1: 方向控制1 (M1_1_Dir)
  - M2: 方向控制2 (M1_2_Dir)
- **PWM速度控制**: GPIO_MOTOR_PWM_C0 (PA12)

#### 电机2 (MOTOR2) - 右轮
- **电源**: 正负极电源供电
- **编码器接口**:
  - A相: PA26 (ENCODER_PIN_A2)  
  - B相: PA27 (ENCODER_PIN_B2)
- **方向控制**:
  - M3: 方向控制1 (M2_1_Dir)
  - M4: 方向控制2 (M2_2_Dir)
- **PWM速度控制**: GPIO_MOTOR_PWM_C1 (PA13)

### 编码器规格
- **分辨率**: 5000线/转
- **编码方式**: AB相正交编码
- **倍频技术**: 4倍频 (每线检测4个边沿)
- **实际分辨率**: 20000脉冲/转

### 控制接口
```c
// 电机控制
void Set_Motor(MOTOTx Motor, int16_t compare);  // PWM范围: -10000~+10000

// 编码器功能
void Get_Encoder_Turns(float* turns);     // 获取电机转数
void Reset_Encoder_Count(void);           // 重置编码器计数  
void Get_Encoder(int16_t* speed);         // 获取编码器增量
```

### 使用示例
```c
// 小车前进
Set_Motor(MOTOR1, 5000);   // 左轮正转
Set_Motor(MOTOR2, 5000);   // 右轮正转

// 小车后退
Set_Motor(MOTOR1, -3000);  // 左轮反转
Set_Motor(MOTOR2, -3000);  // 右轮反转

// 小车左转
Set_Motor(MOTOR1, -2000);  // 左轮反转
Set_Motor(MOTOR2, 2000);   // 右轮正转

// 停车
Set_Motor(MOTOR1, 0);
Set_Motor(MOTOR2, 0);
```

## 硬件控制代码思路

### 步进电机控制(ZDT)
- 参考Hardware/ZDT/Emm_V5.c实现
- 主要功能：使能、加速、停止
- 串口通信控制

### 串口通信
- uart2: 上位机调试通信
- 其他串口: 电机控制等硬件通信

## 代码要求
- 串口调试信息需要尽量简洁
- 在StartupTask.c中集成新的硬件控制任务
- 遵循现有代码风格和结构
- **舵机控制重要提醒**: 编写舵机控制代码前，务必询问使用的是180°还是270°舵机

## 遇到的Bug记录

### 已解决问题 ✅
1. **舵机角度范围问题**: 原代码限制180°，已修改为支持270°舵机
2. **PWM计算公式**: 原公式`Angle/180*2000+500`已修正为`Angle/270*2000+500`
3. **SERVO3控制**: 确认GPIO_Servo_C2 (GPIOA PIN_15)引脚控制正常

*其他bug信息待记录*